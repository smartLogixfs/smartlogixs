apiVersion: v1
kind: Service
metadata:
  name: db-auth
  namespace: smartlogix
  labels:
    app.kubernetes.io/name: db-auth
    app.kubernetes.io/part-of: smartlogix
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app.kubernetes.io/name: db-auth
  ports:
    - name: pg
      port: 5432
      targetPort: pg
