apiVersion: v1
kind: Service
metadata:
  name: ms-user
  namespace: smartlogix
  labels:
    app.kubernetes.io/name: ms-user
    app.kubernetes.io/part-of: smartlogix
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ms-user
  ports:
    - name: http
      port: 8080
      targetPort: http
