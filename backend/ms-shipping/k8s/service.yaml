apiVersion: v1
kind: Service
metadata:
  name: ms-shipping
  namespace: smartlogix
  labels:
    app.kubernetes.io/name: ms-shipping
    app.kubernetes.io/part-of: smartlogix
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ms-shipping
  ports:
    - name: http
      port: 8080
      targetPort: http
