apiVersion: v1
kind: Service
metadata:
  name: db-shipping
  namespace: smartlogix
  labels:
    app.kubernetes.io/name: db-shipping
    app.kubernetes.io/part-of: smartlogix
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app.kubernetes.io/name: db-shipping
  ports:
    - name: pg
      port: 5432
      targetPort: pg
