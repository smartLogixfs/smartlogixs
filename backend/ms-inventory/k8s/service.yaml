apiVersion: v1
kind: Service
metadata:
  name: ms-inventory
  namespace: smartlogix
  labels:
    app.kubernetes.io/name: ms-inventory
    app.kubernetes.io/part-of: smartlogix
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ms-inventory
  ports:
    - name: http
      port: 8080
      targetPort: http
