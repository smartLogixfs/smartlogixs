apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - configmap.yaml
  # secret.yaml NO se referencia aqui: se crea con `kubectl create secret ... --from-env-file`
  # o se copia desde secret.example.yaml. Ver README de infra/k8s/.
